/**
 * The status an event can be in
 * @type {{CANCELLED: string, CLOSED: string, POSTPONED: string, OPEN: string}}
 */
const EVENT_STATUS = {
    OPEN: 'open',
    CLOSED: 'closed',
    CANCELLED: 'cancelled',
    POSTPONED: 'postponed',
};

export default {
    EVENT_STATUS
};
